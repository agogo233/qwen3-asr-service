qwen-asr==0.0.6
gradio==6.17.3
funasr==1.3.1
onnxruntime

# Web 框架
fastapi
uvicorn[standard]
python-multipart
httpx                   # 兼容层 DashScope file_urls 服务端下载（SSRF 防护）

# 文档中心（/web-ui/docs 服务端 Markdown 渲染）
markdown

# 配置文件（funasr 已传递依赖，此处显式声明）
pyyaml

# 音频处理
soundfile
librosa

# 音频事件标注 PANNs CNN14 前端（logmel，纯 torch 微包，MIT；1:1 复现官方精度）
torchlibrosa

# 说话人分离聚类（AHC/谱聚类，CPU 轻量）
scipy
scikit-learn

# OpenVINO（CPU 推理）
openvino>=2024.0

# 模型管理
huggingface_hub
modelscope

# tokenizers（transformers 传递依赖，显式声明确保版本兼容；缺失或版本不匹配会触发
# transformers.integrations.ggml 导入失败，表现为 "Could not import module 'GGUF_CONFIG_MAPPING'"）
tokenizers>=0.22.0,<=0.23.0
